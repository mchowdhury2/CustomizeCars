<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android" package="com.example.customizecars">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity android:name=".ui.SplashScreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".ui.LoginActivity">
            <intent-filter>
                <action android:name="com.example.customizecars.Login" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.MapsActivity"
            android:label="@string/title_activity_maps"></activity>
        <activity android:name=".ui.DBInsertActivity" />

        <activity android:name=".ui.SelectyearActivity"></activity>
        <activity android:name=".ui.DisplayUsernameActivity" />
        <activity android:name=".ui.DatabaseActivity" />

        <!--
        <activity android:name=".SignupActivity">
            <intent-filter>
                <action android:name="com.example.customizecars.Signup" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
        -->

        <activity android:name="ui.DisplayUsernameActivity" />
        <activity android:name="ui.DatabaseActivity" />
        <activity android:name=".ui.ReviewActivity"></activity>
        <activity android:name=".ui.LocationActivity" />
        <activity android:name=".ui.ExternalActivity" />
        <activity android:name=".ui.InternalActivity" />
        <activity android:name=".ui.InfinityActivity" />
        <activity android:name=".ui.FordActivity" />
        <activity android:name=".ui.ToyotaActivity" />
        <activity android:name=".ui.HondaActivity" />
        <activity android:name=".ui.BmwActivity" />
        <activity android:name=".ui.AcuraActivity" />
        <activity android:name=".ui.MakeActivity" />
        <activity android:name=".ui.StartScreenActivity" />
        <activity android:name=".ui.SettingsActivity" />
        <activity android:name=".ui.SettingsEditActivity" />

    </application>


</manifest>